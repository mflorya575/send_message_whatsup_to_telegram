В виртуальном окружении (djvenv) уже установлены нужные библиотеки: selenium, telegram-send.

Если этих библиотек не будет, то введите команды в терминале: pip install selenium, pip install telegram-send.
